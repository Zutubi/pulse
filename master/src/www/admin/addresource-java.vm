#helpTag("Resource+Repository")
#authorisationTags($principle)
#agentTags($slave)
<html>
<head>
    <title>#wwtext("name=add.java.resource")</title>
    <script type="text/javascript" src="$base/js/main.js"></script>
    <script type="text/javascript">
    <!--
    	function openBrowseFileDialog()
    	{
    	    if ($agentId == -1)
    	    {
    	        openFileDialog('$base/popups/fileDialog.action', 'resource.add.java', 'dir', 'local:///', false, false, false);
    	    }
    	    else
    	    {
    	        openFileDialog('$base/popups/fileDialog.action', 'resource.add.java', 'dir', 'agent://$agentId/', false, false, false);
    	    }
    	    return false;
    	}
    -->
    </script>
</head>
<body>
    #agentTabs("resources")

    <h1>:: #wwtext("name=add.java.resource") ::</h1>

    #parse("/template/includes/actionerrors.vm")

#wwcform("action=addResource.action" "id=resource.add.java" "heading=%{getText('resource.add.java')}" "method=post")
    #wwhidden("name=state")
    #wwhidden("name=submit")
    #wwtextfield("name=dir" "label=%{getText('base.dir')}" "size=40" "tabindex=1" "after= <a href='#' onclick='return openBrowseFileDialog();'>browse</a>" "onkeypress=return submitenter(this,event)")
    #previousFinishCancel(2)
#end

#tabEnd()
</body>
</html>
