#helpTag("Plugins+Tab")
#authorisationTags($principle)
<content tag="selectedTab">administration</content>
<html>
<head>
    <title>#wwtext("name=plugins")</title>
</head>
<body>
    #adminTabs("plugins")

    <h2>Installed plugins</h2>

        <table class="fill-content">
            #headingRow("plugins" 6)
            <tr>
                #contentHeading("name" 1)
                #contentHeading("id" 1)
                #contentHeading("state" 1)
                #contentHeading("actions" 3)
            </tr>
            #foreach($plugin in $plugins)
            <tr>
                #if ($plugin.enabled)
                    #set($cssClass = "success")
                #else
                    #set($cssClass = "failure")
                #end
                #classCell($plugin.name)
                #classCell($plugin.id)
                #classCell($plugin.state.toString().toLowerCase())
                #if ($plugin.enabled)
                    #dynamicLinkCell("disable" "disablePlugin.action?id=$plugin.id")
                #end
                #if ($plugin.disabled)
                    #dynamicLinkCell("enable" "enablePlugin.action?id=$plugin.id")
                #end
                #dynamicLinkCell("uninstall" "uninstallPlugin.action?id=$plugin.id")
            </tr>
            #end
        </table>

    <h2>Install a new plugin</h2>
    <p>
        #wwcform("action=installPlugin.action" "name=plugin.install" "heading=%{getText('plugin.install')}" "method=post" "enctype=multipart/form-data")
            #wwfile("name=plugin" "label=path" "required=true" "tabindex=1")
            #upload(2)
        #end
    </p>



</body>
</html>
