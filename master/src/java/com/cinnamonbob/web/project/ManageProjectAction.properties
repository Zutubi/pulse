# Tab bar items
current-build.tab=current build
history.tab=history
configuration.tab=configuration

# Current build tab
never.built=this project has never been built

# History tab
search.builds=search builds
recent.builds=recent builds
no.history=this project has no historical builds

# Configuration tab
project.basics=basics

project.buildspecs=build specifications
buildspec.name=name
buildspec.timeout=timeout
buildspec.trigger=trigger
buildspec.add=add new build specification

project.triggers=triggers
trigger.name=name
trigger.type=type
trigger.spec=spec
trigger.add=add new trigger

project.scm=scm

project.specifics=specifics

project.cleanup=cleanup policy
project.work.dirs.deleted=working directories deleted
project.results.deleted=build results deleted
