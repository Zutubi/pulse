add.project=add project

## Step 1: basics
project.basics=project basics (1 of 3)
project.type.help=for simple projects, select a specific type.  for flexibility, select 'custom project'.

# Step 2: SCM, shared
scm.path=checkout path
scm.path.help=path, relative to working directory, to checkout to (if in doubt leave blank)

# Step 2a: perforce
perforce.setup=perforce setup (2 of 3)
p4.port=port
p4.port.help=the same server:port value as used for the P4PORT variable
p4.user=user
p4.password=password
p4.client=client specification
p4.client.help=client specification (workspace) to use as a template for checkout

# Step 2b: cvs
cvs.setup=cvs setup (2 of 3)
cvs.root=root
cvs.module=module
cvs.password=password

# Step 2c: svn
svn.setup=subversion setup (2 of 3)
svn.username=username
svn.password=password
svn.url=url
svn.keyfile=private key file
svn.keyfile.help=if using ssh key authentication, full path to the private key file to use
svn.passphrase=key file passphrase
svn.passphrase.help=if using an encrypted private key file, the key file passphrase

# Step 3a: custom
custom.setup=custom setup (3 of 3)

# Step 3b: ant
ant.setup=ant setup (3 of 3)

# Step 3c: maven
maven.setup=maven setup (3 of 3)
