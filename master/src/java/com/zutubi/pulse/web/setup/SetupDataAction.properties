# -validation.xml error messages.
data.error=please specify a data directory

# form fields
data.field=pulse data directory

# setup page content.
data.setup=setup pulse data directory

data.help=the data directory will be used to store all data files, e.g. build history
