label=triggers

#
# individual step labels
#
triggerConfig.select.label=select
triggerConfig.label=configure

#
# individual step descriptions
#
triggerConfig.select.description=select the type of trigger you wish to configure
triggerConfig.description=fill in the trigger configuration details below

option.label=trigger type