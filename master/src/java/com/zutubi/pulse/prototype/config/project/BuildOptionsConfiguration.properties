label=build options
introduction=the build options defines a set of generic build options

form.heading=update your build options
form.description=use the form below to update your build options

isolateChangelists.label=isolate changelists
prompt.label=prompt
retainWorkingCopy.label=retain working copy
timeout.label=timeout