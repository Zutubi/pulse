#
# individual step labels
#
zutubi.commitConfig.select.label=select
zutubi.commitConfig.label=configure

option.label=type

#
# individual step descriptions
#
zutubi.commitConfig.select.description=select the transformer type
zutubi.commitConfig.description=configure this commit message transformer
