#
# Overview documentation
#
label=artifact wizard
introduction=artifacts define files and directories that are captured and optionally processed by pulse during a build

option.label=artifact type

#
# individual step labels
#
zutubi.artifactConfig.select.label=select
zutubi.artifactConfig.label=configure

#
# individual step descriptions
#
zutubi.artifactConfig.select.description=select the type of artifact you wish to configure
zutubi.artifactConfig.description=fill in the details below to configure the artifact
