# Form fields
failOnError.label=fail on error
failOnError.help=if true, and the hook task fails then the build will be failed (does not apply \
  when the hook is triggered manually)

# Actions
enable.label=enable
disable.label=disable
