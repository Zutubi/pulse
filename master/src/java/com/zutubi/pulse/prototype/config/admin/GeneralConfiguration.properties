#
# Overview Documentation.
#
label=email configuration
#introduction=
#summary=

#
# Field documentation
#
#    <fieldName>.description
#    <fieldName>.hint
#    <fieldName>.default
#    <fieldName>.label
#

baseUrl.label=base url
baseUrl.hint=the base url to use in urls pointing to this server (e.g. http://[server name]:8080)

baseHelpUrl.label=help base url

rssEnabled.label=rss enabled
rssEnabled.hint=allow users to subscribe to rss project rss feeds. please be aware that these feeds are currently not authenticated.

anonymousAccessEnabled.label=allow anonymous access
anonymousSignupEnabled.label=allow anonymous signup

scmPollingInterval.label=scm polling interval

recipeTimeoutEnabled.label=enable recipe request timeout
recipeTimeoutEnabled.hint=if checked, recipe requests will be timed out when no capable agent is online for a defined period

recipeTimeout.label=recipe request timeout
recipeTimeout.hint=a value of 0 will cause requests to be rejected immediately

#
# Form error messages
#


