label=logging configuration
introduction=the logging configuration settings are used to control how pulse logs errors, warning and \
  other information to disk. Log messages are written to the directory PULSE_HOME/system/log, in rotated \
  log files. Recent error and warning messages can also see seen on the server messages page.

summary=settings controlling how pulse™ logs messages (e.g. errors).

level.label=logging level
level.help=the logging level controls the verbosity of messages logged to disk.

eventLoggingEnabled.label=event logging enabled