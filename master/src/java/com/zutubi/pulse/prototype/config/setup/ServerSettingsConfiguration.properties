label=server settings

#
# Form labels
#
baseUrl.label=base url
baseUrl.description=the base url to use in urls pointing to this server (e.g. http://[server name]:8080)
fromAddress.label=from address
fromAddress.description=the from address for emails sent by this server
prefix.label=subject prefix
prefix.description=the prefix added to the subject of all emails sent by this server
smtpHost.label=smtp host
smtpHost.description=the smtp server to use for sending email
smtpSSL.label=connect via ssl
smtpSSL.description=note that you may need to import the server certificate into a local key store to use ssl
username.label=username
username.description=if the smtp server requires authentication, the username to log in as
password.label=password
password.description=if the smtp server requires authentication, the password for the given user
smtpPort.label=port
smtpPort.description=the smtp server port
smtpCustomPort.label=custom smtp port
smtpCustomPort.description=use a custom port for the smtp service

#
# Error messages
#
smtpHost.required=smtp host is required
fromAddress.required=from address is required
fromAddress.invalid=invalid email address