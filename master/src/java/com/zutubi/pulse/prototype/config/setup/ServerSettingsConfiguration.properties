label=server settings


baseUrl.label=base url
baseUrl.description=the base url to use in urls pointing to this server (e.g. http://[server name]:8080)

from.label=from address
from.description=the from address for emails sent by this server

subjectPrefix.label=subject prefix
subjectPrefix.description=the prefix added to the subject of all emails sent by this server

host.label=smtp host
host.description=the smtp server to use for sending email

ssl.label=connect via ssl
ssl.description=note that you may need to import the server certificate into a local key store to use ssl

username.label=username
username.description=if the smtp server requires authentication, the username to log in as

password.label=password
password.description=if the smtp server requires authentication, the password for the given user

port.label=port
port.description=the smtp server port

customPort.label=custom smtp port
customPort.description=use a custom port for the smtp service


host.required=smtp host is required
from.required=from address is required
from.invalid=invalid email address