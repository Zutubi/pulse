<?xml version="1.0"?>
<project defaultRecipe="default">

    <junit.pp name="junit.pp"/>

    <recipe name="default">
        <command name="build">
            <!-- pull in the maven2 resource if configured. -->
            <resource name="maven2" required="false"/>
            <maven2
#if($workingDir)
                working-dir="${base.dir}/$workingDir"
#end
#if($goals)
                goals="$goals"
#end
#if($arguments)
                args="$arguments"
#end
            />
            <dir-artifact name="test reports" base="${base.dir}/target/surefire-reports" fail-if-not-present="false">
                <include pattern="**/TEST-*.xml"/>
                <process processor="${junit.pp}"/>
            </dir-artifact>
        </command>
    </recipe>
</project>
