<html>
<head>
    <title>create-scm</title>
</head>
<content tag="selectedTab">create-scm</content>
<body>

    #parse("/template/includes/actionerrors.vm")
    
#bodytag (Form "action='createScm.action'" "method='post'")  
    
    #tag (Hidden "name='id'")
        
    <fieldset>
        #tag (TextField "name='svn.name'")
        #tag (TextField "name='svn.username'")
        #tag (TextField "name='svn.password'")
        #tag (TextField "name='svn.url'")
        #tag (TextField "name='svn.path'")
        #tag (TextField "name='svn.keyfile'")
        #tag (TextField "name='svn.passphrase'")
    </fieldset>
        
    <fieldset class="submit">
        #tag (Submit "value=getText('cancel.name')" "name='cancel'")
        #tag (Submit "value=getText('save.name')" "name='save'")
    </fieldset>

 #end 

</body>
</html>
