# Generic internationalised strings, used by default
# If something is used in enough places, whack it in here.

empty.cell=&nbsp;

#
# Generic terms used all over
#
actions=actions
edit=edit
user=user
view=view
scm=scm
error.messages=error messages
warning.messages=warning messages

#
# Forms
#
cancel.name=cancel
create.name=create
edit.name=edit
save.name=save
search.name=search
update.name=update
next.name=next
previous.name=previous
finish.name=finish

#
# Project related terms used in various places
#
project=project
projects=projects
project.name=name
project.description=description
project.type=type

bob.filename=cinnabo file name
bob.filename.help=path of the cinnabo file relative to the working directory

ant.filename=build file
ant.filename.help=path, relative to the working directory, of the ant build file to use
ant.targets=targets
ant.targets.help=space separated list of targets to build (leave blank to build default target)

maven.targets=targets
maven.targets.help=space separated list of targets to build (leave blank to build default target)
maven.base.dir=base directory
maven.base.dir.help=TODO

#
# Project tab bar items
#
current-build.tab=current build
history.tab=history
configuration.tab=configuration

#
# Build related terms
#
build.id=build id
build.status=status
build.spec=spec
build.summary=summary
build.when=when
build.elapsed=elapsed
build.features=features
recipe=recipe
command=command
artifact=artifact

#
# SCM related terms
#
scm.type=type
scm.location=location
scm.path=checkout path
scm.path.help=path, relative to working directory, to checkout to (if in doubt leave blank)

p4.port=port
p4.port.help=the same server:port value as used for the P4PORT variable
p4.user=user
p4.password=password
p4.client=client specification
p4.client.help=client specification (workspace) to use as a template for checkout

cvs.root=root
cvs.module=module
cvs.password=password

svn.username=username
svn.password=password
svn.url=url
svn.keyfile=private key file
svn.keyfile.help=if using ssh key authentication, full path to the private key file to use
svn.passphrase=key file passphrase
svn.passphrase.help=if using an encrypted private key file, the key file passphrase

#
# Build specifications
#
spec.name=name
spec.recipe.help=recipe to build, or blank to build the default recipe
enable.timeout=enable build timeout
spec.timeout=timeout after
spec.timeout.minutes=minutes

################################
# ITEMS BELOW TO BE MOVED OUT
################################

#
# generic stuff
#
spec=build&nbsp;specification

#
# PROJECT
#
bobFileName=bob file

#
# Schedule
#
schedule.name=name
schedule.recipe=recipe

#
# User
#
user.login=login
user.name=full name

#
# Contact Point
#
contact.name=name
contact.email=email address

#
# Trigger
#
trigger.type=type
trigger.cron=<a href="http://quartz.sourceforge.net/javadoc/org/quartz/CronTrigger.html">cron expression</a>

#
# Slave
#
slave.name=name
slave.host=host
slave.port=port

#
# Build specification
#
specification.name=name

#
# Build specification node
#
buildHost=build host
stage.recipe=recipe
