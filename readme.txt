The structure of this directory is as follows:

    build: is used as scratch space for the build process.

    misc: miscellaneous bits and pieces.

    lib: contains all of the dependant packages necessary to build and run this project.

    src: contains the systems source code.


You can build the project by exeuting the default ant build target