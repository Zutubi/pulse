<!--
Create an object that contains the error details of an action.  The object created is of the following format:

object: {
    actionErrors : ['errorA', 'errorB', ..., 'errorX']
    actionMessages : ['messageA', 'messageB', ..., 'messageX']
    fieldErrors : {
        'fieldNameA' : ['errorA', ..., 'errorZ']),
        ...
        'fieldNameZ' : ['errorA', ..., 'errorZ'])
    }
}

The actionErrors and actionMessages are simple arrays. The fieldErrors is an object with fieldNames referencing
an array of error messages specific to that field.

-->
<object>
    <pair key="actionErrors">
        <array ref="actionErrors"></array>
    </pair>
    <pair key="actionMessages">
        <array ref="actionMessages"></array>
    </pair>
    <pair key="fieldErrors">
        <map ref="fieldErrors">
            <array/>
        </map>
    </pair>
</object>