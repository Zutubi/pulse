<?xml version="1.0"?>
<project defaultRecipe="ant build">

        <junit.pp name="junit"/>

    <recipe name="ant build">
        <command name="build">
            <!-- pull in the ant resource if configured. -->
            <resource name="ant" required="false"/>
            <ant
                    >
                            </ant>

                <artifact name="JUnit XML report" file="reports/TESTS-TestSuites.xml" fail-if-not-present="false"
                type="text/xml"
                >
                <process processor="${junit}"/>
    </artifact>
    
        </command>
    </recipe>
</project>
