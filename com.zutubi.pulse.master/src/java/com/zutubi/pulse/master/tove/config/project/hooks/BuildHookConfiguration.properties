label=build hook
introduction=Build hooks are used to run tasks on the Pulse master at certain points in a build.  For example, you \
  can use a hook to tag your source post-build.

name.verbose=A descriptive name for the hook.

# Actions
trigger.label=trigger

# Wizard
wizard.step.select.label=select hook type
wizard.step.select.description=Choose the type of build hook to configure.
wizard.select.label=hook type
wizard.step.configure.label=configure hook
wizard.step.configure.description=Configure build hook details.
