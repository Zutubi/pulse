label=server settings
introduction=optional settings to get you started
verbose=<h3>email setup</h3>\
        <p>You can optionally enter your SMTP server details here, so your Pulse server will immediately be ready to \
        send build notifications.</p>\
        <p>Alternatively you can leave these settings blank for now and fill them in later in the settings &gt; email \
        configuration section of the administration interface.</p>

baseUrl.label=base url
baseUrl.description=the base url to use in urls pointing to this server (e.g. http://[server name]:8080)
