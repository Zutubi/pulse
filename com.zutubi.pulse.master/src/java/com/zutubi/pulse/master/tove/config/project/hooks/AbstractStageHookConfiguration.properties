applyToAllStages.label=apply to all stages
applyToAllStages.verbose=If checked, this hook will be run after every build stage.

stages.label=apply to selected stages
stages.verbose=Specific stages after which to execute this hook.

runTaskOnAgents.label=run task on agents
runTaskOnAgents.verbose=If checked, the task for this hook will be run on the agents that the corresponding stages run \
  on (normally all hook tasks are run on the master).  This does not apply to all tasks, but does apply to the run \
  executable task.
