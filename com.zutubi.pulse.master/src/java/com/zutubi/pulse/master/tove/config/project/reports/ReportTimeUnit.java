package com.zutubi.pulse.master.tove.config.project.reports;

/**
 * Available time units for selecting builds to report over.
 */
public enum ReportTimeUnit
{
    /**
     * Used to select a fixed number of builds.
     */
    BUILDS,
    /**
     * Used to select builds from a fixed number of days.
     */
    DAYS
}
