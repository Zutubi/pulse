label=dependency
introduction=A dependency defines another project that this project depends upon to be able to build.  

# while we only reference projects within Pulse, the org field is somewhat redundant since all
# of our projects are uniquely named.  However, when we begin supporting external repositories
# (necessary for supporting external ivy configurations) we will need to define an organisation.
# org.label=org
# org.verbose=The organisation

module.label=project

project.label=project
project.verbose=The dependent project

revision.label=revision
revision.verbose=The revision of the dependency.  

transitive.label=transitive
transitive.verbose=Marking this dependency as transitive indicates that this dependencies own \
  dependencies will be resolved when this dependency is resolved.

stages.label=stages