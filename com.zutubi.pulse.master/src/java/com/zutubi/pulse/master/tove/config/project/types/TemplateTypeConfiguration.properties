postProcessors.label=output post-processors
postProcessors.verbose=Post-processors to apply to the output of the build command. \
  These processors can be used to find features (e.g. error messages) in the build \
  output.  Note that some commands automatically apply post-processing (e.g. the ant \
  command automatically applies the ant output processor).

