label=project dependencies

introduction=The project dependencies defines a locally configured set of projects that this project \
  depends upon.  The dependent projects publications are retrieved at build time and are made \
  available to this projects build.

dependencies.label=dependencies
dependencies.verbose=The list of dependent projects