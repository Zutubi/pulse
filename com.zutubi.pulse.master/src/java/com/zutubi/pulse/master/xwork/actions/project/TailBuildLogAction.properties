select.log=select log
build.log=build log
stage.log=stage {0} log

download.full.log=download full log

settings.max.lines=maximum of {0} lines
settings.refresh.interval=refresh every {0} seconds

tail.box=tail of log
