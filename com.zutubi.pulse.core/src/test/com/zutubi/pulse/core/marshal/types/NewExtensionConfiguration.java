package com.zutubi.pulse.core.marshal.types;

import com.zutubi.tove.annotations.SymbolicName;

/**
 * An extension of the extendable configuration which is registered after the
 * docs are first initialised (to simulate a plugin).
 */
@SymbolicName("newExtension")
public class NewExtensionConfiguration extends ExtendableConfiguration
{
}