label=resource version
introduction=Resource versions allow you to support multiple versions of a single resource.
verbose=Resource versions allow you to support multiple versions of a single resource.  This is \
  especially useful when you need to test against different versions of some dependency.  By \
  requiring different versions of a resource, the same recipe can be used to test against multiple \
  dependency versions.

value.label=resource version
value.verbose=The version string.
value.example.1=2000 SP1
value.example.2=1.2.55

value.required=value is required