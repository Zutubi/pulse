introduction=Defines a set of recipes for a project.
verbose=Defines the available recipes for building a project.  Each build stage may, if desired, \
  build a different recipe.

defaultRecipe.label=default recipe
defaultRecipe.verbose=Name of the recipe to execute if no recipe is explicitly specified.
