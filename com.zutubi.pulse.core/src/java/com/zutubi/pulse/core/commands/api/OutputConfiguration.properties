label=output capture
introduction=Files, directories and reports to capture as part of the build result.
verbose=Output captures are used to capture output files, directories and reports produced by \
  the build to be stored as part of the build result.  These reports can be processed to extract \
  features or test results, and can be browsed as part of the build.

wizard.step.select.label=select capture type
wizard.step.select.description=Choose the type of output capture.
wizard.step.configure.label=configure capture
wizard.step.configure.description=Configure the selected type of output capture.
