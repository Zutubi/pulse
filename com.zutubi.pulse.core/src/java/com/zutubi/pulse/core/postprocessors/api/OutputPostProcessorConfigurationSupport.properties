failOnError.label=fail build on error
failOnError.verbose=If true, any error features detected will cause the command (and hence build) \
  to fail.

failOnWarning.label=fail build on warning
failOnWarning.verbose=If true, any warning features detected will cause the command (and hence \
  build) to fail.
