label=version
introduction=A version string to attach to builds that include this recipe.
verbose=Defines a version string to attach to builds of this recipe.  May be based on a property. \
  Note that for some types of projects the version may be extracted automatically.

value.label=version string
value.verbose=The version string to attach to the build.