<?xml version="1.0" encoding="ISO-8859-1"?>
<?xml-stylesheet type="text/xsl" href="http://www.jayasoft.fr/org/ivyrep/ivy-doc.xsl"?>
<ivyconf>

    <conf defaultResolver="resolvers" checkUpToDate="true"/>

    <resolvers>

        <dual name="resolvers">
            <chain name="ivy">
                <filesystem name="ivy-repository">
                    <ivy pattern="${ivy.conf.dir}/ivy-repository/[organisation]/[module]/ivy-[revision].xml"/>
                </filesystem>
            </chain>
            <chain name="artifact">
                <filesystem name="local-repository" checkmodified="true">
                    <artifact pattern="e:/work/repository/[organisation]/[module]/[type]s/[artifact]-[revision].[type]"/>
                </filesystem>
                <url name="artifact-repository">
                    <artifact pattern="http://220.233.100.30/repository/[organisation]/[module]/[type]s/[artifact]-[revision].[type]"/>
                </url>
            </chain>
        </dual>

        <filesystem name="build" checkmodified="true">
            <ivy pattern="${ivy.conf.dir}/build/repository/[module]/ivy-[revision].xml"/>
            <artifact pattern="${ivy.conf.dir}/build/repository/[module]/[type]s/[artifact]-[revision].[type]"/>
        </filesystem>

    </resolvers>

    <modules>
        <!-- we know where all of the pulse packages are located, no
             point searching through the others -->
        <module organisation="zutubi" name=".*" resolver="build"/>
    </modules>

</ivyconf>

