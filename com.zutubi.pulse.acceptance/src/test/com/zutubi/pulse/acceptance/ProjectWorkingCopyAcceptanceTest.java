package com.zutubi.pulse.acceptance;

/**
 * Tests for the working copy functionality.
 */
public class ProjectWorkingCopyAcceptanceTest
{
    // working copy link should only be shown when
    // a) we have an incremental checkout scheme
    // b) user has permissions to view source



    // working copy browser should work for
    // a) builds on master agent
    // b) builds on remote agent

    // and handle the other cases
    // a) no build has been run, so no working copy is available.
    // b) agent is offline so working copy can not be retrieved.
}
