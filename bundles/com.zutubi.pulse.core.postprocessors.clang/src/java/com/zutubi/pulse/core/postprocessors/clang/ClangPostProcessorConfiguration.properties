label=clang output processor
introduction=A pre-configured regular expression processor for finding errors and warnings in clang \
  output.
verbose=A regular expression processor that is pre-configured with patterns for matching clang error \
  and warning messages.

example.trivial.blurb=A trivial clang post-processor
example.apply.blurb=Apply a clang post-processor to an executable command (e.g. a script that calls \
  make/clang):
