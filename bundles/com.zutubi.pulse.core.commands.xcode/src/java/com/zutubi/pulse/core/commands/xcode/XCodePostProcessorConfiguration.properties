label=xcode output processor
introduction=A pre-configured regular expression processor for finding errors in xocdebuild output.
verbose=A regular expression processor that is pre-configured with patterns for matching common \
  xcodebuild error and warning messages.  Note that xcode commands apply this processing by \
  default, so there is no need to explicitly add this processor in that case.

example.trivial.blurb=A trivial xcodebuild post-processor
example.apply.blurb=Apply a make post-processor to an executable command (e.g. a script that calls \
  xcodebuild):
