label=junit summary output processor
introduction=A pre-configured regular expression processor for reporting test status messages \
  in JUnit summary output.
verbose=A regular expression processor that is pre-configured with patterns for matching test \
  status messages in JUnit summary output.  Note that the test results themselves are not captured \
  by this processor.
