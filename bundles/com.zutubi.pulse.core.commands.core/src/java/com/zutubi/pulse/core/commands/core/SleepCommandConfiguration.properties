label=sleep command
introduction=A command to pause the build for a number of milliseconds.
verbose=A simple command that will pause the build for a given interval when executed.  Primarily \
  useful for debugging.

interval.label=interval (ms)
interval.verbose=The number of milliseconds to sleep for.
interval.min=interval must not be negative

example.simple.blurb=Sleep for 10 seconds
