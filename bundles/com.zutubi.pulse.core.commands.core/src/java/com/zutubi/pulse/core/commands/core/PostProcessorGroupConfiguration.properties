label=post-processor group
introduction=A group of post-processors that can be applied as a single processor.
verbose=A post-processor group is used to create a single post-processor out of multiple simpler \
  post-processors. The group consists of zero or more child post-processors which will each be \
  applied in order when the group is applied.

processors.label=processors

example.apply.blurb=A processor that searches for compiler errors and also gathers test results \
  from command output
