label=custom field command
introduction=A command used to add simple custom properties.
verbose=The custom field command can be used to add simple custom property values to the recipe or \
  build result.

example.simple.blurb=Add a codename field to the build result
