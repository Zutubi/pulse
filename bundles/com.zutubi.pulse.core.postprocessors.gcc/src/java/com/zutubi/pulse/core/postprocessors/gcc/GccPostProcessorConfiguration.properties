label=gcc output processor
introduction=A pre-configured regular expression processor for finding errors and warnings in gcc \
  output.
verbose=A regular expression processor that is pre-configured with patterns for matching gcc error \
  and warning messages.

example.trivial.blurb=A trivial gcc post-processor
example.apply.blurb=Apply a gcc post-processor to an executable command (e.g. a script that calls \
  make/gcc):
