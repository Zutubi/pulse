label=visual studio output processor
introduction=A pre-configured regular expression processor for finding errors and warnings in \
  Visual Studio tool output.
verbose=A regular expression processor that is pre-configured with patterns for matching error and \
  warning messages produced by Visual Studio tools.  This includes various compilers, the linker \
  and other tools.

example.trivial.blurb=A trivial visual studio post-processor
example.apply.blurb=Apply a visual studio post-processor to an executable command (e.g. a script \
  that calls visual studio tools):
